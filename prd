小学数学课堂互动绘图工具 - PRD文档
1. 项目背景与目标
1.1 背景
在小学数学教学中，平面图形（如长方形、圆形、树叶轮廓）的周长测量是一个重要知识点。传统的PPT展示缺乏互动性，而普通绘图软件无法精确测量手绘轨迹的长度。
老师急需一款工具，能让学生在大屏幕（或电子白板）上亲手描摹实物图片的轮廓，直观地理解“描边长度”与“图形周长”的概念，并能对比曲线与直线段长度的区别。
1.2 目标
开发一款基于Web的轻量级工具，老师上传图片后，学生可以通过手指或鼠标在图片上进行描边、绘图，支持曲线/直线互转，并能实时显示并累加所画路径的长度，将抽象的“周长”转化为可视化的数字。

---
2. 用户角色与使用场景
- 用户角色：小学数学老师（操作者）、小学生（互动者）
- 使用场景：
  - 老师课前上传一张树叶图片，上课时让学生描出树叶的轮廓，软件自动计算树叶的“周长”。
  - 老师画一个不规则的多边形，让学生点击“转直线”功能将弯曲的边拉直，计算其总长度，理解“化曲为直”的数学思想。

---
3. 功能需求详述
3.1 画布与图片管理模块
- F-01：图片上传
  - 描述：支持点击上传或拖拽上传本地图片（JPG, PNG, WebP）。
  - 要求：上传后，图片自动适配画布宽度，保持宽高比，背景色设为白色或网格底纹。
- F-02：画布缩放与移动（老师专用）
  - 描述：由于是课堂大屏幕，老师可能需要对图片细节进行放大描边。
  - 交互：支持双指捏合缩放（触屏）或滚轮缩放（鼠标）；按住空格键拖拽移动画布。
3.2 绘图核心模块
- F-03：手绘描边（自由画笔）
  - 描述：学生手指或鼠标按下后，跟随移动轨迹绘制线条。
  - 参数：线条默认颜色为红色（醒目），线宽建议默认 4px（在大屏幕上足够清晰），支持颜色和粗细调节（可选功能）。
- F-04：连续直线绘制
  - 描述：点击“直线模式”，点击画布生成锚点，自动连接上一个锚点，双击或按ESC结束当前线段组。
3.3 核心数学转换功能（本项目的亮点）
- F-05：曲线转直线
  - 描述：选中之前画好的某一段手绘曲线，点击“转为直线”按钮。
  - 逻辑：系统识别该段曲线的起点和终点坐标，将中间的复杂曲线替换为一条笔直的线段。
  - 教学意义：直观演示“化曲为直”的过程，方便对比长度变化。
- F-06：直线转曲线
  - 描述：选中某一条直线，点击“转为曲线”按钮。
  - 逻辑：保留起点和终点，在直线中点增加一个可拖拽的控制点（贝塞尔曲线控制柄），老师/学生可以拖拽中点使其弯曲。
  - 教学意义：演示“直变曲”后长度增加的现象。
3.4 测量与计算模块
- F-07：实时长度计算
  - 描述：自动计算当前选中线条的长度。
  - 单位：像素（px） 或 物理单位（厘米/英寸）。
    - 逻辑：由于上传的图片大小不一，单纯显示像素可能对小学生不够直观。
    - 建议方案：提供一个 “标定”功能。老师在图片上先画一个已知长度的参照物（例如在尺子图片上描出1厘米），系统计算像素与厘米的换算比例。之后所有画的图形都显示“XX 厘米”。
- F-08：总长度累加
  - 描述：在画布侧边栏显示“当前选中图形长度”和“页面所有图形总长度”。
  - 教学意义：方便计算复杂组合图形的总周长。
3.5 编辑与辅助模块
- F-09：图形选择与拖动
  - 描述：点击“选择工具”，点击画布上的线条，线条高亮显示（加粗或显示控制点），出现操作菜单（转换、删除、设置颜色）。
- F-10：撤销/重做
  - 描述：支持 Ctrl+Z 或点击按钮撤销上一步操作。课堂互动中难免画错，这是刚需。
- F-11：清空画布
  - 描述：一键清除所有绘制的图形，保留背景图片。
- F-12：保存/导出
  - 描述：将当前画布（包含图片+所有绘制的图形）导出为 PNG 图片，方便老师保存学生的课堂练习成果。

---
4. 交互与UI要求
- 大屏适配：
  - 按钮需要足够大（建议最小点击区域 44x44pt），间距足够，避免学生手指误触。
  - 工具栏采用底部悬浮或侧边栏折叠式设计，避免遮挡绘图区域。
- 视觉反馈：
  - 当点击“转为直线”时，线条应有一个平滑的动画过渡，而不是突然变化，帮助学生理解变换过程。
  - 当计算长度时，数字应有明显的闪烁或放大效果，吸引学生注意力。

---
5. 非功能需求
- 性能：
  - 纯前端实现（HTML5 Canvas + JavaScript），无需后端服务器，保护学生隐私。
  - 手绘描边需流畅，无延迟（帧率 > 30fps）。
- 兼容性：
  - 支持 Chrome / Edge 浏览器。
  - 重点优化触屏体验：由于是在大屏幕上用，需要处理 touchmove 事件的默认行为（防止页面滚动）。
- 技术栈建议：
  - Canvas：用于绘图和显示。
  - Fabric.js 或 Paper.js：比较推荐使用这类矢量图形库。因为本PRD的核心需求是“选中物体”、“转换曲线”、“计算长度”，矢量图形库可以很方便地将手绘笔触转换为可编辑的路径对象，相比纯Canvas像素绘图更容易实现“曲线转直线”这类复杂操作。

---
6. 界面原型建议（草图描述）
- 顶部栏：[上传图片] [撤销] [清空]
- 左侧/右侧工具栏：
  - [画笔/描边工具]（手绘模式）
  - [直线工具]（点击画点模式）
  - [选择工具]（箭头图标）
  - [颜色/粗细]（可选）
- 右侧信息面板：
  - 当前选中：长度：XX 厘米 (或 px)
  - 总长度：XX 厘米 (或 px)
  - [转为直线] 按钮 (选中曲线时高亮)
  - [转为曲线] 按钮 (选中直线时高亮)
- 底部：提示语：“用手指描边，点击线条可以转直/转曲哦！”

---
7. 逻辑难点说明
在开发时需要关注以下逻辑细节：
1. 手绘路径的处理：
手绘时收集的点非常多，直接存储会生成很复杂的路径。转为直线时，需要忽略中间的所有点，只保留起点和终点。
2. 长度计算的精确性：
  - 曲线长度：需要对手绘路径进行采样积分计算。
  - 直线长度：勾股定理。
  - 单位换算：如果使用“标定”功能，需要计算一个全局的 px_to_cm_ratio。